#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int a = 11, b = 19;
    // int ans = a & b; // AND operator
    // int ans = a | b; // OR operator
    // int ans = a ^ b; // XOR operator
    // int ans = a << 1; // left shift
    int ans = 14 >> 2; // right shift

    cout << (1LL << 40) << endl;

    cout
        << ans << endl;

    return 0;
}